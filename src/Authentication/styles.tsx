import * as CSS from 'csstype';

export const dialogText: CSS.Properties = {
    textTransform: 'unset',
    textAlign: 'center',
    marginBottom: '10px',
};

export const spaceAbove: CSS.Properties = {
    marginTop: "10px"
}

export const dialogDivider: CSS.Properties = {
    marginTop: "15px",
    marginBottom: "15px",
}