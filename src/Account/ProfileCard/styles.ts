import * as CSS from 'csstype';

export const profileFormElementStyle: CSS.Properties = {
    display: 'flex',
    alignItems: 'baseline',
    marginTop: '10px',
}

export const profileFormTextStyle: CSS.Properties = {
    marginRight: '4px',
}